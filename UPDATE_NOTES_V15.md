# V15 - B tv+ 화면 자동 분석 연결

- 홈 상단에 `현재 B tv+ 화면 분석하기` 버튼 추가
- 별도 `B tv+ 화면 분석` 페이지 추가
- JSON 복사/붙여넣기 UI 제거
- Chrome 확장프로그램에서 현재 페이지를 읽어 Streamlit 분석 페이지로 자동 전달
- 감지된 블록명, 순서, 콘텐츠 제목 표시
- 테마 DB에서 배치할 테마를 선택하면 추천 위치 3곳 자동 계산
- 확장프로그램은 `chrome_extension` 폴더에 포함
