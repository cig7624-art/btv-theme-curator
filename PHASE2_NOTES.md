# Phase 2 · 키노라이츠 실시간 콘텐츠 후보

## 바뀐 구조

- `theme_pool.csv` / `theme_db.csv`: 테마 라이브러리로 계속 사용합니다.
- `content_db.csv`: 기본 추천 풀에서 제외했습니다.
- 추천 콘텐츠 후보: 테마별 검색어를 만들고 키노라이츠 GraphQL 검색 결과를 합쳐 선별합니다.
- `kinolights_content_cache.csv`: 앱이 처음 검색하면 자동 생성되는 14일 캐시입니다.

## 이번주 추천 테마

1. 최근 이슈와 테마 DB를 먼저 매칭해 상위 테마를 고릅니다.
2. 상위 테마만 키노라이츠에서 병렬 검색합니다.
3. 최근 이슈의 실제 작품명, 테마명, 핵심 키워드를 최대 5개 검색어로 사용합니다.
4. 검색어별 최대 12개 결과를 합치고 중복을 제거합니다.
5. 여러 검색어에서 반복 발견된 콘텐츠를 우선하고, 같은 시리즈·연대 쏠림을 완화합니다.
6. 최종 5~20편만 화면에 표시합니다.

## 테마 DB 전체 보기

- 500개 테마를 한꺼번에 키노라이츠에서 조회하지 않습니다.
- 자연어로 테마를 찾고, 선택한 테마 하나에 대해 `키노라이츠 후보 불러오기`를 누릅니다.

## 기존 콘텐츠풀 보완

- 기본값은 꺼짐입니다.
- 키노라이츠 장애 또는 검색 결과 부족을 확인하기 위한 비교용 옵션으로만 남겼습니다.
- 이 옵션을 켜야만 `content_db.csv`가 부족한 수량을 보완합니다.

## 현재 한계

키노라이츠의 현재 검색은 제목 중심입니다. 따라서 `호프`, `인터스텔라`처럼 실제 작품명 검색은 잘 맞지만 `마음이 따뜻해지는 이야기`, `왕의 곁에서 본 역사`처럼 추상적인 문장은 결과가 적거나 부정확할 수 있습니다. 이번 버전은 실제 결과를 먼저 확인하기 위한 1차 적용본이며, 다음 보정에서 검색어 생성과 후보 재평가를 강화합니다.
