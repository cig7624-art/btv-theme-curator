# V11 UI 업데이트

- 테마 카드의 콘텐츠 새로고침을 Streamlit fragment로 분리해 전체 화면 이탈과 스크롤 점프를 줄였습니다.
- 카드별 새로고침 후 `st.rerun()`을 호출하지 않습니다.
- 최초 구축 중에는 `다음 미연결 테마 20개 콘텐츠 연결`만 표시합니다.
- 520개 전체 연결이 끝난 뒤에만 `30일 이상 지난 테마 20개 콘텐츠 업데이트`가 표시됩니다.
- 각 테마 카드에 슬레이트 테두리와 파란 왼쪽 포인트를 추가해 블록 구분을 강화했습니다.
